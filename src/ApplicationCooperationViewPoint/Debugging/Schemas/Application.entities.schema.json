{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "id": "http://Pickup.fr/carto/schemas/entities/Application",
  "type": "object",
  "description": "Application",
  "required": [
    "Target"
  ],
  "properties": {
    "Target": {
      "type": "string",
      "pattern": "^[a-zA-Zéèà\\s'/Éœô.-]+[a-zA-Z0-9_éèà\\s'/Éœô.-]+$"
    },
    "Referentials": {
      "items": {
        "$ref": "#/definitions/Application"
      }
    }
  },
  "definitions": {
    "Application": {
      "type": "object",
      "required": [
        "Name",
        "Label",
        "Id"
      ],
      "properties": {
        "Name": {
          "type": "string",
          "description": "Specify the functional name of the entity",
          "pattern": "^[a-zA-Zéèà\\s'/Éœô.-]+[a-zA-Z0-9_éèà\\s'/Éœô.-]+$"
        },
        "Label": {
          "type": "string",
          "description": "Specify the label for this entity",
          "pattern": ""
        },
        "Description": {
          "type": "string",
          "description": "Specify the description for this entity",
          "pattern": ""
        },
        "Id": {
          "type": "string",
          "description": "Specify the key of the entity",
          "pattern": ""
        },
        "Technologie": {
          "type": "string",
          "description": "Technologie of the application"
        },
        "Trigram": {
          "type": "string",
          "description": "Spécifie le trigramme de d'application"
        },
        "Type": {
          "type": "string",
          "description": "Spécifie le type d'application",
          "enum": [
            "Partenaire",
            "Interne",
            "SaaS",
            "Bureautique",
            "Utilisateur",
            "Infrastructure",
            "Progiciel"
          ]
        }
      }
    }
  }
}